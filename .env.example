# ===========================================
# Environment Variables (copy to .env)
# ===========================================

# Browser to use for testing (chrome, firefox, edge)
BROWSER=chrome

# Run tests in headless mode
HEADLESS=false

# Test data file path (override default)
# TEST_DATA_PATH=src/test/resources/testdata/TestData.xlsx
